# Checklisten

Vor Call: User klar, History, Draft-Kanten, Reason, dry_run.
Nach dry_run: DRY_RUN, dann Write-Key.
Nach Live: UPDATED + History. Kein Vertrag aus MCP.
Deploy: bindings_inherit, /health, Count > 0.
