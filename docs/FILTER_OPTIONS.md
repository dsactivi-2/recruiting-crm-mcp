Siehe vollständige Tabelle in diesem Repo unter docs — Filter 1–28 in der RPC, 29–70 geplant.

RPC:

```
crm_api.search_candidates_filtered(p_filters jsonb, p_query text, p_limit int, p_cursor int)
crm_api.count_candidates_filtered(p_filters jsonb, p_query text)
crm_api.get_candidate(p_candidate_id int)
```
