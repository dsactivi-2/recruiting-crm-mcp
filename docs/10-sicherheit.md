# Sicherheit

Nie in Tools: kandidat_jmbg, kandidat_password, kandidat_brojlk, kandidat_broj_pasosa.

RLS deny anon/authenticated. Zugriff nur SECURITY DEFINER. search_path fest. Filter-Whitelist. LIKE mit Escape. Limit max 50.

Agent: kein execute_sql zum Suchen. Status nur auf klare Anweisung. Keine Dumps in diesem Repo.
